# Source via 1Password — never commit real values
# op run --env-file=.env.local -- npm run dev

# Required for /api/v1/runs scope creation (Milestone 2+)
HYPERDRIVE_ID_CHITTYOS_CORE=

# Optional — for evidence + ledger projection (Milestone 4+)
HYPERDRIVE_ID_CHITTYEVIDENCE=
HYPERDRIVE_ID_CHITTYLEDGER=

# Service tokens (via wrangler secret put)
CHITTY_AUTH_SERVICE_TOKEN=
